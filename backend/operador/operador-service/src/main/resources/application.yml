server:
  port: 8082

spring:
  application:
    name: operador
  datasource:
    url: jdbc:mysql://mysql.railway.internal:3306/railway
    username: userop
    password: Operador
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false

app:
  cors:
    allowed-origins: "*"
