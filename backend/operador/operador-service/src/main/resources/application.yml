server:
  # Railway asigna PORT dinámico. Local usa 8082.
  port: ${PORT:8082}

spring:
  application:
    name: operador

  datasource:
    # NO pongas usuario:password en la URL. Formato correcto: jdbc:mysql://host:3306/db
    # Prioridad:
    # 1 SPRING_DATASOURCE_URL (ya en formato jdbc:mysql://...)
    # 2 Construido con MYSQLHOST / MYSQLPORT / MYSQLDATABASE expuestos por Railway
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://root:hXEdvyGFqEUpLowQmCjKPYiYftWIXYiZ@mysql.railway.internal:3306/railway}
    username: ${SPRING_DATASOURCE_USERNAME:approot}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: false

# CORS simple (ajusta con tu dominio de Vercel luego)
app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:*}