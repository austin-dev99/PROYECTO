server:
  port: ${PORT:8080}

spring:
  application:
    name: buscador

eureka:
  client:
    enabled: false

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:*}
  elasticsearch:
    url: ${ELASTICSEARCH_HOST}
    apiKey: ${ELASTIC_API_KEY}
  operador:
    url: https://operador-production.up.railway.app/productos
  gateway:
    url: ${GATEWAY_URL}

#  ${OPERADOR_URL}


